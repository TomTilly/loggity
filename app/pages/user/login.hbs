<div class="accounts">
  <form class="accounts__form"
        action="/user/login"
        method="post">
    <h3 class="accounts__heading">Log in</h3>
    {{#if flashErrors }}
    {{#each flashErrors }}
    <div class="flash flash--error">{{ this }}</div>
    {{/each}}
    {{/if}}
    {{#if flashSuccesses }}
    {{#each flashSuccesses }}
    <div class="flash flash--success">{{ this }}</div>
    {{/each}}
    {{/if}}
    <div class="text-field">
      <input class="text-field__input"
             type="text"
             name="username"
             id="username"
             required>
      <label class="text-field__label"
             for="username">Username</label>
    </div>
    <div class="text-field">
      <input class="text-field__input"
             type="password"
             name="password"
             id="password"
             required>
      <label class="text-field__label"
             for="password">Password</label>
    </div>
    <button class="btn btn--secondary btn--full-width accounts__submit"
            type="submit">Log
      in</button>
    <div class="accounts__forgot-link-container">
      <a class="accounts__forgot-link"
         href="/user/forgot">Forgot your
        password?</a>
    </div>
  </form>
</div>